
# brainstorming visual companion
.superpowers/
docs/superpowers/
graphify-out/
dist/
bin/
# Release-time shell completions and man pages, generated by tools/genassets.
.assets/
coverage.out
# `ofga mapping init` writes these into the working directory, so running the
# wizard from the repo root leaves them here. Anchored to the root: the catalog
# ships its own internal/mapping/auth0/model.fga, which is tracked.
/mapping.yaml
/model.fga
